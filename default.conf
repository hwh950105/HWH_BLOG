server {
    listen 80;
    listen [::]:80;

    server_name test123444.com;

    root /var/www/html;
    index index.html index.htm;

    location / {
        try_files $uri /index.html;
    }

    # 📌 /api/ 요청을 3000번 포트의 Node.js로 전달
    location /api/ {
        proxy_pass http://127.0.0.1:3000;  # Node.js 서버로 프록시
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
# scp default.conf hwangwh@192.168.0.14:/home/hwangwh/nginx-config/default
# sudo mv /home/hwangwh/nginx-config/default /etc/nginx/sites-available/default
# sudo ln -sf /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default
# sudo nginx -t  # 설정 오류 확인
# sudo systemctl restart nginx  # Nginx 재시작